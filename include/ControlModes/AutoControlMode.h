#ifndef AUTOCONTROLMODE_H
#define AUTOCONTROLMODE_H

namespace AutoControlMode {
    extern void autoMode();
    extern void auto_send_features();
    extern void auto_send_btns();
}

#endif // AUTOCONTROLMODE_H