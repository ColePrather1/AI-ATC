cmake_minimum_required(VERSION 3.12)
project(AI_ATC CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Find required packages
find_package(SDL2 REQUIRED)
find_package(SQLite3 REQUIRED)
#find_package(Threads REQUIRED)

# Add RF24 library (assuming it's in a subdirectory)
add_subdirectory(RF24)
# Add RF24 library (assuming it's installed in a standard location)
#find_library(RF24_LIBRARY rf24)


# Include directories
include_directories(${CMAKE_SOURCE_DIR}/include)



# Add source files
set(SOURCES
    src/main.cpp
    src/RXThread.cpp
    src/TXThread.cpp
    src/SQLLoggerThread.cpp
    src/ComputationThread.cpp
    src/GameControllerThread.cpp
)


# Create executable
add_executable(${PROJECT_NAME} ${SOURCES})
# Add your source files
#add_executable(MyProject src/main.cpp)


# Link libraries
target_link_libraries(${PROJECT_NAME}
    PRIVATE
    Threads::Threads
    SQLite::SQLite3
    SDL2::SDL2
    #${RF24_LIBRARY}
)
#target_link_libraries(MyProject PRIVATE
#    SDL2::SDL2
#    SQLite::SQLite3
#    RF24
#)



# Include directories
#target_include_directories(MyProject PRIVATE include)



# Install the executable
#install(TARGETS ${PROJECT_NAME} DESTINATION bin)
